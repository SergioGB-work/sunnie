script(id="templateThemeList",type="text/x-jsrender")
	| {{if data.length > 0 }}
	| {{for data}}
	| {{* window.id = parseInt(Math.random() * (99999 - 0) + 0) }}
	.col-4.form-group
		div(class="custom-control custom-radio")
			input(type="radio" class="custom-control-input" id="theme{{*:id}}" name="theme" value="{{:}}")
			+fragment('fragment-label',{"for":"theme{{*:id}}", "text":"{{:}}", "class":"custom-control-label"},'true')
	| {{/for}}
	| {{/if}}